# Mili Packaging 独立站运营工作台 · 质检审计与优化方案

> 审计对象：`public/admin.html`（单文件 SPA，4315 行，14 个功能标签页）+ `deploy.cjs`（Worker API 层）+ `src/data/products.json`（数据层，9 组 / 73 产品）
> 审计日期：2026-08-29 · 方法：逐模块代码审查 + 数据链路核对 + Worker API 语义核对
> 数据事实：73 个产品全部已具备 title / description / faq / tierPrice（数据层完整，SEO 数据基础好）

---

## 一、总体评价

**做得好的部分**
- 架构克制：单文件零依赖 SPA，无需构建，Worker 直出，适合当前规模。
- 数据口径统一：询盘中心以 Supabase `inquiries` 表为唯一数据源，渠道/状态/优先级/跟进字段齐全，已对齐 WhatsApp/邮件/网站/聊天/电话多通道。
- 闭环完整：产品编辑 → GitHub Contents API 写入 → Actions 自动部署（约 2 分钟），已覆盖产品/分组/图片上传/AI SEO 字段应用。
- 状态可视化：配置进度环、今日优先任务、同步状态条、连通性测试、SLA 徽章等，后台专业度在同类工具中属于中上水平。

**主要短板（一句话总结）**
- **功能未闭环**：店铺装修、账号管理为占位页；图片 alt 编辑是"假功能"；编辑抽屉无法改 SEO 字段（title/description/faq），导致 SEO 医生"去修复"跳转后无路可修。
- **编辑能力与数据层脱节**：73 个产品数据层字段很全，但编辑器只能编辑其中 7 个字段，且存在"图片清空不生效、copy 清空不生效"的真实保存缺陷。
- **数据展示有失真**：全站时间以 UTC 原样展示（北京时间差 8 小时）；编辑抽屉实时 SEO 分用伪对象计算，已达标产品显示"待优化"。
- **异常处理偏弱**：看板同步无超时、询盘 PIN 明文前端、危险操作缺少二次确认、部分接口无服务端校验。
- **效率工具不足**：询盘无排序/导出/今日待跟进清单，报表无环比与渠道转化率对比，产品列表无分页与"只看缺口"筛选。

---

## 二、问题清单（按模块，含严重度与定位）

### A. 功能完整性缺口（"能看但做不了"）

| # | 级别 | 问题 | 定位 |
|---|---|---|---|
| A1 | **P0** | 编辑抽屉只有 name/tagline/spec/copy[0]/img/price/moq/order/status，**无法编辑 title（SEO 标题）、description（Meta）、faq、关键词**。而 SEO 评分正好扣这些字段 → SEO 医生"去修复"跳到编辑器后无字段可改（死路），只能依赖 AI 应用 | admin.html:998-1123（抽屉字段区）；seoScore 扣分项在 3624-3641 |
| A2 | **P0** | **图片 alt 编辑与批量生成不持久化**：alt 弹层保存仅关闭+toast+标记脏（307-312 行），`edGetAllImages` 恒返回 `alt:''`，`批量生成 alt` 只弹提示不写数据 → 属"假功能" | admin.html:2594-2602, 2704-2725 |
| A3 | **P0** | **图片清空/删除附图无法保存**：保存时 `if (extra.length) fields.extraImgs = extra`、`if (img) fields.img = img`（2843-2850），为空则不发送；服务端 `Object.assign(prod, fields)` 合并（deploy.cjs:258）→ 旧图永远残留。**copy 清空同理不生效** | admin.html:2842-2850；deploy.cjs:252-259 |
| A4 | **P1** | 店铺装修 tab 为占位：保存按钮 `alert('即将上线')`，site-title/desc/whatsapp/email 四个输入框无任何读写逻辑 | admin.html:1445-1484 |
| A5 | **P1** | 账号管理 tab 为占位：`account-add-btn` 无事件绑定（死按钮）；权限分级仅文案说明；且文案写"登录密码 mili2026"——mili2026 是询盘 PIN，与登录密码体系（ADMIN_PW_HASH）不一致，误导 | admin.html:1487-1518 |
| A6 | **P1** | 询盘中心无 CSV 导出/导入；无独立"客户列表"视图（只能从单条询盘打开画像抽屉）；报表无导出 | admin.html:3191-3240 |
| A7 | **P1** | 编辑器规划中的 M3 变体参数 / M5 内容 SEO / M6 状态版本+CSV 导入导出 未落地（任务 124-126 仍 pending）；复制产品不复制 title/description/faq → 复制品 SEO 分骤降 | admin.html:2183-2220 |
| A8 | **P2** | 批量操作两处重复实现：编辑器底部批量条（ed-batch-bar）与后台工具批量区（batchArea），功能重叠、口径可能不一致 | admin.html:918-923 vs 1244-1258 |

### B. 操作效率问题

| # | 级别 | 问题 | 定位 |
|---|---|---|---|
| B1 | **P1** | 询盘表无列排序（时间/金额/SLA）、无跟进人（owner）筛选、无固定列；13 列表格在 1366px 屏必横向滚动 | admin.html:3002-3058 |
| B2 | **P1** | 跟进提醒只有颜色标注，无"今日待跟进"清单聚合；PushPlus 已配置却未接入询盘 SLA 推送 | admin.html:3013-3014 |
| B3 | **P1** | **SLA 计算基于 created_at 而非最后活动时间**：已报价/已联系 3 天的询盘仍显示"超48h"红色，误导优先级 | admin.html:2904（inqAge 用 created_at） |
| B4 | **P2** | 搜索无防抖：询盘搜索每按键全量重渲染 500 行表格；产品搜索每按键重建列表 | admin.html:2988, 1792 |
| B5 | **P2** | 产品列表 73 行全渲染，无分页/虚拟滚动；无"只看缺口"快捷筛选（缺 Meta / 缺附图 / 低分 / 下架） | admin.html:2222-2310 |
| B6 | **P2** | 单条上架/下架、询盘行内状态切换均**无二次确认**（批量/归档有 confirm，单条没有） | admin.html:2313-2320, 3045 |
| B7 | **P2** | 草稿仅存基础字段，不含阶梯价/区间价/附图行修改 | admin.html:2477-2486 |
| B8 | **P3** | 防重复提交依赖第二个监听器的禁用时序（ed-save 双监听 2811 + 4282）；保存超 8s 兜底会提前解锁，慢网络下仍可能双发 | admin.html:4282-4288 |

### C. 数据展示问题（"数据不准/误导"）

| # | 级别 | 问题 | 定位 |
|---|---|---|---|
| C1 | **P1** | **全站时间显示为 UTC 原样**：`inqT`/`fmtTime` 直接 `String(s).replace('T',' ').slice(0,16)`，Supabase/GitHub 返回 ISO UTC → 北京时间差 8 小时。询盘表、看板部署/提交时间、工单历史全部中招 | admin.html:1591, 2905 |
| C2 | **P1** | **编辑抽屉实时 SEO 分失真**：`edUpdateScore` 用伪对象（无 title/description/faq/tierPrice，2426-2447），已完全达标的产品打开抽屉显示约 65 分"待优化"，与列表页分数矛盾 | admin.html:2426-2447 vs 3624-3641 |
| C3 | **P2** | 询盘报表（inqRenderReport）无时间范围/渠道/跟进人筛选、无环比（本月 vs 上月）、无渠道转化率对比（每渠道 won/总数） | admin.html:3191-3240 |
| C4 | **P2** | 统计卡无趋势（本月 vs 上月、环比箭头）；成交额 `value_usd` 无时间维度 | admin.html:2957-2975 |
| C5 | **P2** | 看板"最近部署"显示的是任务**开始时间**（created_at）而非完成时间；GitHub 未鉴权 API 限流 60/h，失败仅提示不重试 | admin.html:1593-1604 |
| C6 | **P3** | `countUpEl`/`__countUpEl` 为死代码，看板 KPI 未使用 | admin.html:2040-2053 |

### D. 异常处理与安全

| # | 级别 | 问题 | 定位 |
|---|---|---|---|
| D1 | **P1** | 询盘 PIN（mili2026）明文写在前端 JS 且账号页明文展示——纯装饰性保护，懂代码即可绕过；应改为 Worker 会话级解锁 | admin.html:2888, 1508 |
| D2 | **P1** | `refreshDash` 的 `Promise.all` 无超时：任一数据源挂起（如 GitHub 限流等待），同步状态卡"同步中"；`fetch` 无 AbortController | admin.html:1697-1717 |
| D3 | **P2** | `/api/content` 无字段白名单与服务端校验（status 任意字符串、price 任意格式均可写入）；并发编辑时 sha 冲突 409 无自动重试；无撤销能力 | deploy.cjs:240-274 |
| D4 | **P2** | 后台工具"价格乘数"用 `parseFloat(p.price)`：区间价字符串 "0.49-5.99 FOB" 被解析为 0.49 → 批量后**毁掉区间价格式** | admin.html:4240-4262 |
| D5 | **P2** | SEO 导出 CSV 未转义引号/换行/公式前缀（`=`/`+`/`@`）→ 存在 CSV 注入风险 | admin.html:3997-4003 |
| D6 | **P3** | `edBatchSetStatus` 引用不存在的 `ed-batch-apply` 节点（死代码，当前靠新按钮工作，但隐患在） | admin.html:2172 |

### E. 其他

| # | 级别 | 问题 |
|---|---|---|
| E1 | P2 | 登录密码体系文档不一致：账号页写"密码 mili2026"，实际登录走 ADMIN_PW_HASH（当前默认 1127 的哈希）；生产环境已配置自定义哈希，1127 登录被拒属预期行为，但文档与 UI 需统一 |
| E2 | P2 | 审计日志仅内存 500 条（冷启动重置），无询盘操作审计（谁改了状态/金额不可追溯） |
| E3 | P2 | GEO 页承诺"每周自动查询 AI 平台检测品牌词"，但工作台内未见到对应 cron 任务与 API，当前实际只有静态 sitemap 缺口匹配——文案需与实现对齐 |
| E4 | P3 | 登录页 account 字段仅校验邮箱格式，不校验账号是否存在（任何邮箱+正确密码即管理员）——单密码模式的预期行为，但"账号管理"页给人多账号错觉 |
| E5 | P3 | 询盘加载 `limit=500` 硬上限，超 500 条后旧数据不可见，且无"加载更多" |

---

## 三、优化方案（按批次落地，均可独立交付）

### 批次 1：P0 功能闭环 + 显示修复（半天工作量，收益最大）

| 编号 | 动作 | 落地要点 |
|---|---|---|
| F1 | 抽屉补 SEO 字段区 | 新增 title / description / faq（3-5 条问答式编辑器）/ keywords 字段；保存时并入 `fields`；复制产品时复制这些字段（修 A1/A7 一半） |
| F2 | alt 真正持久化 | 数据层图片从字符串升级为 `{src, alt}`（兼容旧字符串）；`edGetAllImages` 读真实 alt；alt 弹层保存写回本地态并在保存时下发；批量生成 alt 写入每张图（修 A2） |
| F3 | 支持清空字段 | 保存语义改为"显式值覆盖"：空附图发送 `extraImgs: []`、空 copy 发送 `copy: null`（或服务端对显式空数组/空串做删除）；服务端 `Object.assign` 前过滤 `null` 值字段（修 A3） |
| F4 | 抽屉 SEO 分合并真实数据 | `edUpdateScore` 合并当前产品真实 title/description/faq/tierPrice + 表单现值再评分（修 C2） |
| F5 | 全站时间本地化 | `fmtTime`/`inqT` 改用 `new Date(s).toLocaleString('zh-CN', {hour12:false})` 或手动 +8h 格式化；SLA 计算改 `updated_at`（修 C1/B3） |

### 批次 2：询盘中心数据能力（用户重点关注的"数据查看与操作"）

| 编号 | 动作 | 落地要点 |
|---|---|---|
| F6 | 询盘表升级 | 表头可点击排序（时间/金额/SLA/状态）；筛选栏加"跟进人"输入；13 列默认隐藏次要列 + 列显隐开关；行内状态切换加确认（won/lost/archived 必确认） |
| F7 | "今日待跟进"清单 | 看板新增卡片：`follow_up_at ≤ 今日` 且状态未终结的询盘列表，点击直达询盘中心并自动筛选；SLA 超 48h 标红 |
| F8 | PushPlus SLA 推送 | 复用现有 worker cron（每日简报），在简报中追加：待跟进 Top10、超 48h 清单、今日新增询盘摘要；可选每小时级别 |
| F9 | 报表增强 | 报表页加时间范围/渠道筛选；渠道卡加"转化率（won/总数）"；新增"跟进人漏斗"（每人 new→won 数量）；全部支持 CSV 导出（含转义修复 D5） |
| F10 | 客户视图 | 新增"客户"子页：按邮箱/电话/公司聚合客户（复用画像抽屉逻辑），展示询盘数/成交/金额/最近活动，点击进画像 |
| F11 | 分页与加载更多 | `limit=500` 改为按需加载（offset 分页 + 计数），或至少在 UI 标注数据上限 |

### 批次 3：效率与安全加固

| 编号 | 动作 | 落地要点 |
|---|---|---|
| F12 | 搜索防抖 + 列表缺口筛选 | inq-f-q / ed-search 加 300ms 防抖；产品列表加"缺口"下拉（缺附图/缺 Meta/低分<70/下架中） |
| F13 | 同步超时 | `refreshDash` 每个 fetch 包 `AbortController`（12s 超时），失败项标记并可单独重试 |
| F14 | PIN 服务端化 | 新增 `/api/inquiry/unlock`（POST PIN，Worker 内存/会话标记），前端不再存明文；账号页移除 PIN 明文展示，改为"询盘中心访问密码在设置中管理" |
| F15 | /api/content 校验与并发 | 服务端字段白名单 + status/price/name 格式校验；PUT 409 冲突时自动重读 sha 重试 1 次；支持 `fields: {copy: null}` 删除语义 |
| F16 | 批量工具收敛 | 合并编辑器与后台工具两处批量入口（保留一处），价格乘数仅对纯数字价格生效，区间价产品跳过并提示（修 A8/D4） |
| F17 | 危险操作确认 | 单条上架/下架加确认；批量操作前弹影响数预览（"将下架 N 个产品"） |

### 批次 4：后续规划（对应 pending 任务 124-126）

| 编号 | 动作 | 说明 |
|---|---|---|
| F18 | M3 变体参数 | 抽屉增加规格参数区（尺寸/材质/工艺/印刷选项），写入数据层 spec/specs 字段 |
| F19 | M5 内容 SEO | 编辑器增加长描述段落管理、内链产品推荐、FAQ 编辑（与 F1 合并实现） |
| F20 | M6 状态版本 + CSV | 产品变更版本留痕（在 products.json 或 ops/ 下存历史快照）；产品 CSV 导入导出 |
| F21 | M7 询盘联动 | 编辑产品时显示该产品相关询盘数/金额；询盘产品字段与产品 slug 关联（当前为自由文本） |
| F22 | 通知中心 | 统一收口：部署失败/死链/SLA 超时/审计告警聚合页，替代散落 toast |
| F23 | 驾驶舱询盘组合视图 | 渠道×状态×金额矩阵、环比（近 7 天 vs 前 7 天）、成交转化率趋势 |
| F24 | 代码清理 | 死代码（countUpEl/ed-batch-apply/旧兼容容器）、重复 RAW fetch 缓存、decor/accounts 占位页移除或落地 |

---

## 四、验收清单（每批次交付后逐项勾选）

- [ ] F1：抽屉可编辑并保存 title/description/faq；SEO 医生"去修复"后可手动改全部扣分项
- [ ] F2：编辑 alt → 保存 → 重新打开抽屉 alt 仍在；前台 img alt 属性生效
- [ ] F3：删除全部附图并保存后，前台产品只剩主图；清空描述并保存后前台无该段
- [ ] F4：打开已达标产品，抽屉 SEO 分与列表页一致（≥85）
- [ ] F5：询盘表/看板时间显示为北京时间；SLA 按最后活动时间计算
- [ ] F6-F11：排序/筛选/导出/客户视图/待跟进清单可用，数据一致
- [ ] F12-F17：防抖生效；同步有超时兜底；危险操作有确认；PIN 不在前端明文
- [ ] 回归：产品保存发布、批量上下架、图片上传、AI 字段应用、驾驶舱刷新均不回归

---

## 五、关于"数据查看与操作功能性"的专项建议（本次重点）

1. **统一"运营三视图"**：数据看板（昨日/近 7 天经营摘要）→ 询盘中心（执行层，今日待跟进为默认视图）→ 数据驾驶舱（GA4/GSC 流量侧），三者在顶部互相跳转，避免"看数要开三个页面"。
2. **所有数字可点击下钻**：看板"待跟进 N"→ 询盘中心自动带状态筛选；"成交 X%"→ 报表页；"下架中 N"→ 产品列表带状态筛选。
3. **时间维度默认相对化**：所有列表时间显示"x 分钟前 / 昨天 14:32"，悬停显示完整时间，符合运营扫视习惯。
4. **一键导出中心**：询盘（含筛选结果）、产品、SEO 审计、配置状态，统一 CSV 入口与转义规范。
5. **操作留痕闭环**：产品/分组已有审计，补询盘变更审计（状态/金额/跟进人），可在审计页按对象筛选。

---

*报告由代码级审查生成；F 系列方案均基于现有架构（无需引入新框架/新服务），可分批独立实施并随 GitHub Actions 自动部署。*
